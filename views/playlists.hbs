{{> welcomemenu}}

<section class="ui center aligned middle aligned segment">
  {{#each playlists}}
    <section class="ui raised segment">
      <h2 class="ui header">
        {{title}}
      </h2>
      {{> list}}
    </section>
  {{/each}}
</section>