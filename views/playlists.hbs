{{> welcomemenu}}

<section class="ui divided list segment">
  {{#each playlists}}
    <div class="item">
      <i class="large browser middle aligned icon"></i>
      <div class="content">
        <a href="/playlist/{{@index}}" class="header">{{title}}</a>
      </div>
    </div>
  {{/each}}
</section>